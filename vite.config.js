import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// Configurazione Vite per l'applicazione di dimensionamento climatizzazione
export default defineConfig({
  plugins: [react()],
});
